'use client';

import { BasicEmailInput } from './BasicEmailInput';

export const BasicEmailInputAlternativeFloatingLabelClearable = (props: any) => (
  <BasicEmailInput {...props} theme='alternative' floatingLabel='Your Email' isClearable />
);
