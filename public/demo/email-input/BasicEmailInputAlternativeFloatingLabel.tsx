'use client';

import { BasicEmailInput } from './BasicEmailInput';

export const BasicEmailInputAlternativeFloatingLabel = (props: any) => (
  <BasicEmailInput {...props} theme='alternative' floatingLabel='Your Email' />
);
