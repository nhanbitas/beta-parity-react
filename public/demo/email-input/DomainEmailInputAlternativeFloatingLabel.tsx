'use client';

import { DomainEmailInput } from './DomainEmailInput';

export const DomainEmailInputAlternativeFloatingLabel = (props: any) => (
  <DomainEmailInput {...props} theme='alternative' floatingLabel='Your Email' />
);
