'use client';

import { FileItem } from 'beta-parity-react/ui/FileItem';
import React from 'react';

type Props = any;

export const FileItemComponent = (props: Props) => <FileItem className='w-96' />;
