'use client';
import React from 'react';
import { Textarea } from 'beta-parity-react/ui/Textarea';

export const BasicTextAreaRows0 = () => (
  <Textarea wrapperProps={{ className: '!max-w-96' }} rows={0} placeholder='Enter your message' />
);
