'use client';
import React from 'react';
import { DatePicker } from 'beta-parity-react/ui/DatePicker';

type Props = any;

export const DatePickerColorAccentSideIconLeftInputSizeSmThemeAlternative = (props: Props) => (
  <DatePicker color='accent' sideIcon='left' inputSize='sm' theme='alternative' {...props} />
);
