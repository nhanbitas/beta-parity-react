'use client';
import React from 'react';
import { DatePicker } from 'beta-parity-react/ui/DatePicker';

type Props = any;

export const DatePickerColorAccentSideIconLeftThemeAlternative = (props: Props) => (
  <DatePicker color='accent' sideIcon='left' theme='alternative' {...props} />
);
